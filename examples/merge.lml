open Std.Printf
open Std.List ((:), map)
open Std.Pipe ((|>))
open Std.Arith ((*))

let merge la ra =
  when (la, ra) is
    | ([], []) = []
    | (h:r, c:t) = h:c:merge r t

let main () = 
  1:2:3:4:5:[]
  |> merge [6, 7, 8, 9, 10]
  |> map (\x -> x * 2)
  |> Printf.println

